# Template file for 'noctalia'
# Run ./pkgs/noctalia/update to set version to the latest main commit.
pkgname=noctalia
version=4a2cb7b
revision=1
build_style=meson
buildtype=release
short_desc="Noctalia desktop shell"
maintainer="Noctalia <team@enoctalia.dev>"
license="MIT"
homepage="https://github.com/noctalia-dev/noctalia"

hostmakedepends="meson ninja pkg-config git curl"

makedepends="
 wayland-devel
 wayland-protocols
 libepoxy-devel
 MesaLib-devel
 libglvnd-devel
 cairo-devel
 pango-devel
 fontconfig-devel
 freetype-devel
 harfbuzz-devel
 libxkbcommon-devel
 pipewire-devel
 libcurl-devel
 pam-devel
 libwebp-devel
 basu-devel
 sdbus-c++-devel
 polkit-devel
 librsvg-devel
 libqalculate-devel
 libxml2-devel
"

depends="
 wayland
 pipewire
 pam
 libwebp
 basu
 jemalloc
"

noverify=yes
nosource=yes
CXXFLAGS+=" -std=c++20"

do_fetch() {
	git clone https://github.com/noctalia-dev/noctalia.git --branch main ${wrksrc}
}

post_install() {
	vlicense LICENSE

	if [ ! -x "${DESTDIR}/usr/bin/noctalia" ]; then
		msg_error "Expected binary /usr/bin/noctalia not found.\n"
	fi
}
